// **********************************************Template Literals**************************************************************

// it provide the morden way to handle string in javascript, they use backticks(`)

const product = "Laptop";
const price = 1200;
const message = `The price of the ${product} is $${price}.`;
console.log(message); // "The price of the Laptop is $1200."



const a = 5;
const b = 10;
const result = `The sum of ${a} and ${b} is ${a + b}.`;
console.log(result); // "The sum of 5 and 10 is 15."
